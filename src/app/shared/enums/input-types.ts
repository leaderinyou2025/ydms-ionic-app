export enum InputTypes {
  EMAIL = 'email',
  NUMBER = 'number',
  PASSWORD = 'password',
  SEARCH = 'search',
  TEL = 'tel',
  TEXT = 'text',
  URL = 'url',
  DECIMAL = 'decimal',
  NUMERIC = 'numeric',
}
